# os-multi-container-monitor
A lightweight container runtime built in C with a long-running supervisor and a Linux kernel module for monitoring and enforcing per-container memory limits. The system demonstrates process isolation using namespaces, user–kernel communication via IOCTL, concurrent logging using a bounded buffer, and scheduling experiments to analyze Linux behavior
